_site
.DS_Store
.jekyll
.bundle
.sass-cache
Gemfile
Gemfile.lock
node_modules
package.json
.Rproj.user
.env

# Git worktrees (isolated workspaces)
.worktrees/
scripts/__pycache__/
scripts/tests/__pycache__/
.venv/

# RenderCV scratch output
rendercv_output/

# Sources analyzer output
sources/_diff.md
sources/_processed/

# Vita PDF: generated by `npm run gen:vita` locally and by CI on each deploy
site/public/vita.pdf

# ORCID review-diff (regenerate via orcid_works.py)
orcid_diff.md

# Zenodo review-diff (regenerate via scripts/zenodo_works.py)
zenodo_diff.md

# Profile fill review-diff (regenerate via fill_missing_fields.py)
profile_fill_diff.md

# AI agent context (local only — keep out of history)
CLAUDE.md
